version: "3.3"
services:
  lavalink:
    build: lavalink/
    volumes:
      - ./application.yml:/opt/Lavalink/application.yml
  lavamusic:
    image: ghcr.io/stefanlobbenmeier/lavamusic:dockerfile
    volumes:
      - ./.env:/opt/lavamusic/.env
    links:
      - mongo
      - lavalink
  mongo:
    build: mongo/
    environment:
      MONGO_INITDB_ROOT_USERNAME: mongoadmin
      MONGO_INITDB_ROOT_PASSWORD: mongopassword
